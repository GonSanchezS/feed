<div class="container right-column">

  <p class="date--title">TODAY</p>

  <% if (Date.today).saturday? || (Date.today).sunday? %>
    <p class="weekend--note">The eCommerce robot 🤖 doesn't work on weekends. You shouldn't either 🏄</p>
  <% else %>
    
    <ul>

        <li class="daily--link">
          <a href="https://www.nerdmarketing.com/roas-romi/" class="main--link">ROAS vs. ROMI: This is how you should measure your marketing</a> 📌
            <br />
          <span class="source">Nerd Marketing • Featured</span>
        </li>

      <div sheetsu="https://sheetsu.com/apis/v1.0/a2533b00323b" sheetsu-sheet="Sorted" sheetsu-sheet="Sorted"  sheetsu-search='{"date": "<%= (Date.today).strftime('%B %d') %>"}'>

        <li class="daily--link">
          <a href="{{url}}" class="main--link">{{category}} {{title}}</a>
            <br />
          <span class="source">{{source}} • <a class="twitter-share-button" rel="me"
  href="https://twitter.com/intent/tweet?text={{title}}%20{{url}}%20via%20@ecommercedailyy">
<i class="fa fa-twitter" aria-hidden="true"></i></a></span>
        </li>
      </div>
    </ul>
  <% end %>

    <br />

 <!-- Yesterday -->   

  <% if (Date.today - 1.days).saturday? %> 
  <!-- Nothing -->
  <% elsif (Date.today - 1.days).sunday? %>
      <p class="date--title"><%= (date = Date.today - 1.days).strftime('%B %d') %></p>
      <p class="weekend--note">The eCommerce robot 🤖 doesn't work on weekends. You shouldn't either 🏄</p>  
  <% else %>   
    <p class="date--title"><%= (date = Date.today - 1.days).strftime('%B %d') %></p>
    <ul>
      <div sheetsu="https://sheetsu.com/apis/v1.0/a2533b00323b" sheetsu-sheet="Sorted" sheetsu-search='{"date": "<%= (Date.today - 1.days).strftime('%B %d') %>"}'>
        <li class="daily--link">
          <a href="{{url}}" class="main--link">{{category}} {{title}}</a>
            <br />
          <span class="source">{{source}} • <a class="twitter-share-button" rel="me"
  href="https://twitter.com/intent/tweet?text={{title}}%20{{url}}%20via%20@ecommercedailyy">
<i class="fa fa-twitter" aria-hidden="true"></i></a></span>
        </li>
      </div>
    </ul>
    <br />
  <% end %>


<!-- 2 Days Ago -->   

  <% if (Date.today - 2.days).saturday? %> 
  <!-- Nothing -->
  <% elsif (Date.today - 2.days).sunday? %>
      <p class="date--title"><%= (date = Date.today - 2.days).strftime('%B %d') %></p>
      <p class="weekend--note">The eCommerce robot 🤖 doesn't work on weekends. You shouldn't either 🏄</p>  
  <% else %>   
    <p class="date--title"><%= (date = Date.today - 2.days).strftime('%B %d') %></p>
    <ul>
      <div sheetsu="https://sheetsu.com/apis/v1.0/a2533b00323b" sheetsu-sheet="Sorted" sheetsu-search='{"date": "<%= (Date.today - 2.days).strftime('%B %d') %>"}'>
        <li class="daily--link">
          <a href="{{url}}" class="main--link">{{category}} {{title}}</a>
            <br />
          <span class="source">{{source}} • <a class="twitter-share-button" rel="me"
  href="https://twitter.com/intent/tweet?text={{title}}%20{{url}}%20via%20@ecommercedailyy">
<i class="fa fa-twitter" aria-hidden="true"></i></a></span>
        </li>
      </div>
    </ul>
    <br />
  <% end %>


<!-- 3 Days Ago -->   

  <% if (Date.today - 3.days).saturday? %> 
  <!-- Nothing -->
  <% elsif (Date.today - 3.days).sunday? %>
      <p class="date--title"><%= (date = Date.today - 3.days).strftime('%B %d') %></p>
      <p class="weekend--note">The eCommerce robot 🤖 doesn't work on weekends. You shouldn't either 🏄</p>  
  <% else %>   
    <p class="date--title"><%= (date = Date.today - 3.days).strftime('%B %d') %></p>
    <ul>
      <div sheetsu="https://sheetsu.com/apis/v1.0/a2533b00323b" sheetsu-sheet="Sorted" sheetsu-search='{"date": "<%= (Date.today - 3.days).strftime('%B %d') %>"}'>
        <li class="daily--link">
          <a href="{{url}}" class="main--link">{{category}} {{title}}</a>
            <br />
          <span class="source">{{source}} • <a class="twitter-share-button" rel="me"
  href="https://twitter.com/intent/tweet?text={{title}}%20{{url}}%20via%20@ecommercedailyy">
<i class="fa fa-twitter" aria-hidden="true"></i></a></span>
        </li>
      </div>
    </ul>
    <br />
  <% end %>

  <!-- 4 Days Ago -->   

  <% if (Date.today - 4.days).saturday? %> 
  <!-- Nothing -->
  <% elsif (Date.today - 4.days).sunday? %>
      <p class="date--title"><%= (date = Date.today - 4.days).strftime('%B %d') %></p>
      <p class="weekend--note">The eCommerce robot 🤖 doesn't work on weekends. You shouldn't either 🏄</p>  
  <% else %>   
    <p class="date--title"><%= (date = Date.today - 4.days).strftime('%B %d') %></p>

    <ul>
      <div sheetsu="https://sheetsu.com/apis/v1.0/a2533b00323b" sheetsu-sheet="Sorted" sheetsu-search='{"date": "<%= (Date.today - 4.days).strftime('%B %d') %>"}'>
        <li class="daily--link">
          <a href="{{url}}" class="main--link">{{category}} {{title}}</a>
            <br />
          <span class="source">{{source}} • <a class="twitter-share-button" rel="me"
  href="https://twitter.com/intent/tweet?text={{title}}%20{{url}}%20via%20@ecommercedailyy">
<i class="fa fa-twitter" aria-hidden="true"></i></a></span>
        </li>
      </div>
    </ul>
    <br />
  <% end %>

  <!-- 5 Days Ago -->   

  <% if (Date.today - 5.days).saturday? %> 
  <!-- Nothing -->
  <% elsif (Date.today - 5.days).sunday? %>
      <p class="date--title"><%= (date = Date.today - 5.days).strftime('%B %d') %></p>
      <p class="weekend--note">The eCommerce robot 🤖 doesn't work on weekends. You shouldn't either 🏄</p>  
  <% else %>   
    <p class="date--title"><%= (date = Date.today - 5.days).strftime('%B %d') %></p>

    <ul>
      <div sheetsu="https://sheetsu.com/apis/v1.0/a2533b00323b" sheetsu-sheet="Sorted" sheetsu-search='{"date": "<%= (Date.today - 5.days).strftime('%B %d') %>"}'>
        <li class="daily--link">
          <a href="{{url}}" class="main--link">{{category}} {{title}}</a>
            <br />
          <span class="source">{{source}} • <a class="twitter-share-button" rel="me"
  href="https://twitter.com/intent/tweet?text={{title}}%20{{url}}%20via%20@ecommercedailyy">
<i class="fa fa-twitter" aria-hidden="true"></i></a></span>
        </li>
      </div>
    </ul>
    <br />
  <% end %>

  <!-- 6 Days Ago -->   

  <% if (Date.today - 6.days).saturday? %> 
  <!-- Nothing -->
  <% elsif (Date.today - 6.days).sunday? %>
      <p class="date--title"><%= (date = Date.today - 6.days).strftime('%B %d') %></p>
      <p class="weekend--note">The eCommerce robot 🤖 doesn't work on weekends. You shouldn't either 🏄</p>  
  <% else %>   
    <p class="date--title"><%= (date = Date.today - 6.days).strftime('%B %d') %></p>

    <ul>
      <div sheetsu="https://sheetsu.com/apis/v1.0/a2533b00323b" sheetsu-sheet="Sorted" sheetsu-search='{"date": "<%= (Date.today - 6.days).strftime('%B %d') %>"}'>
        <li class="daily--link">
          <a href="{{url}}" class="main--link">{{category}} {{title}}</a>
            <br />
          <span class="source">{{source}} • <a class="twitter-share-button" rel="me"
  href="https://twitter.com/intent/tweet?text={{title}}%20{{url}}%20via%20@ecommercedailyy">
<i class="fa fa-twitter" aria-hidden="true"></i></a></span>
        </li>
      </div>
    </ul>
    <br />
  <% end %>

</div>
